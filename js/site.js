document.addEventListener('DOMContentLoaded',function(){const buttons=document.querySelectorAll('[data-course-filter]');const cards=document.querySelectorAll('[data-course-category]');buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const filter=btn.dataset.courseFilter;cards.forEach(card=>{card.style.display=(filter==='all'||card.dataset.courseCategory===filter)?'block':'none';});}));const params=new URLSearchParams(window.location.search);const interest=params.get('interest');if(interest){const select=document.querySelector('select[name="program"]');if(select){const option=[...select.options].find(o=>o.value.toLowerCase()===interest.toLowerCase());if(option) select.value=option.value;}}});